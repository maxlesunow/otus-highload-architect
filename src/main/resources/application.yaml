spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  flyway:
    user: ${DB_USERNAME}
    url: ${DB_URL}
    password: ${DB_PASSWORD}
seed:
  enabled: true
  users-count: 1000000